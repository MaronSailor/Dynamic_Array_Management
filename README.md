# Dynamic_Array_Management
The program is made via C++

Creates vector with random numbers and then sorts it. Also can add new number, pop out any element from array, sort array and delete duplicates
